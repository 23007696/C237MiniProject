<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Update Product</title>
</head>

<body>
    <% include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Update Product</h2>

        <form action="/products/<%= category %>/<%= product.id %>/update" method="post" enctype="multipart/form-data">
            <div class="mb-3 mt-3">
                <label for="product">Product Name:</label>
                <input type="text" class="form-control" id="productName" value="<%= product.name %>" placeholder="Enter Product Name" name="name">
            </div>
            <div class="mb-3 mt-3">
                <label for="quantity">Quantity:</label>
                <input type="number" class="form-control" id="quantity" value="<%= product.quantity %>" placeholder="Enter Quantity" name="quantity">
            </div>
            <div class="mb-3 mt-3">
                <label for="price">Price:</label>
                <input type="number" class="form-control" id="price" value="<%= product.price %>" placeholder="Enter Price" name="price" step="0.01">
            </div>
            <div>
                <label for="currentImage">Current Image:</label><br>
                <input type="text" id="currentImage" value="<%= product.image %>" disabled><br><br>
                <img src="/images/<%= product.image %>" width="20%"><br><br>

                <label for="newImage">New Image:</label><br>
                <input type="file" id="newImage" name="image" accept="image/*">
                <br><br>
            </div>

            <button type="submit" class="btn btn-primary">Submit</button>
            <a href="/products/laptops/<%= product.id %>" class="btn btn-secondary">Back</a>
        </form>
        
    </div>
</body>

</html>
