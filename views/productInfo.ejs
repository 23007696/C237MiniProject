<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Product Information</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Product Information</h2>

        <div class="mb-3 mt-3">
            <label for="productName"><b>Product Name: </b></label>
            <%= product.name %>
        </div>
        <div>
            <label for="image"></label>
            <img src="/images/<%= product.image %>" alt="Product Image" width="400" height="300">
        </div>
        <div class="mb-3 mt-3">
            <label for="quantity"><b>Quantity: </b></label>
            <%= product.quantity %>
        </div>
        <div class="mb-3 mt-3">
            <label for="price"><b>Price: </b>$</label>
            <%= product.price %>
        </div>
        <a href="/" class="btn btn-primary">Back</a>
        <a href="/products/<%= category %>/<%= product.id %>/update" class="btn btn-info">Edit</a>
        <form action="/products/<%= category %>/<%= product.id %>/delete" method="post" style="display: inline;">
            <button type="submit" class="btn btn-danger">Delete</button>
        </form>
        <form action="/cart/add" method="post" style="display: inline;">
            <input type="hidden" name="userId" value="1">
            <input type="hidden" name="itemName" value="<%= product.name %>">
            <input type="hidden" name="quantity" value="1">
            <input type="hidden" name="price" value="<%= product.price %>">
            <input type="hidden" name="image" value="<%= product.image %>">
            <button type="submit" class="btn btn-success">Add to Cart</button>
        </form>
    </div>
</body>
</html>
